// SongsView — v2 redesign. Delegates to SongsViewV2 (Amplified Underground theme).
// Kept as a thin re-export so App.tsx import paths don't need to change.
export { SongsViewV2 as SongsView } from './v2/SongsViewV2'
